@import '../../App/mixins'

.section
  position relative

.container
  bottom 0
  color white
  display flex
  flex-direction column
  height 100%
  justify-content flex-start
  left 0
  padding-top 50px
  position absolute
  right 0
  top 0
  +media-lt(sm)
    padding-top 15px
